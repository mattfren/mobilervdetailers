---
import { getQuoteAction, site } from "../data/site";
import TrustChips from "./TrustChips.astro";

const quoteAction = getQuoteAction();
---

<section class="hero">
  <div class="hero-bg" aria-hidden="true">
    <img src={site.images.hero} alt="" width="1400" height="900" loading="eager" fetchpriority="high" />
  </div>
  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow">Canton, TX mobile RV exterior care</p>
      <h1>Premium Mobile RV Detailing in Canton, TX</h1>
      <p class="hero-lede">
        Cleaner RV exterior. No drop-off. Exterior wash, roof or awning care,
        black streak removal, and full exterior detail by quote.
      </p>
      <div class="hero-actions">
        <a
          class="btn btn-red btn-large"
          href={quoteAction.href}
          target={quoteAction.external ? "_blank" : undefined}
          rel={quoteAction.external ? "noreferrer" : undefined}
        >
          {quoteAction.label}
        </a>
        <a class="btn btn-glass btn-large" href="/services/">View Services</a>
      </div>
      <TrustChips />
    </div>
  </div>
</section>
